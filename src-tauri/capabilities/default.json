{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Least-privilege capability set for the main window. Only APIs the frontend actually calls are enabled — the app never opens a shell, walks the filesystem, or writes files from the JS side (Rust owns all persistence).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
